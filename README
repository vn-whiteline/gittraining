TRAIING GIT @ STAMP HQ
=========
git init
git config user.name="vn-whiteline"
git config user.email="vn@whiteline-group.com"
git status
git add README
git commit -m "Create Project"

ssh-keygen -t rsa
git remote add origin git@github.com:vn-whiteline/TrainGit.git
git remote set-url origin get@github.com:vn-whiteline/TrainGit.get
git remote -v

Conflick
